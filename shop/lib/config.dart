/// Config for firebase backend.
///
/// This Config class holds information for the Firebase backend.
/// To run this app, you'd need to create an own Firebase project
/// (see https://console.firebase.google.com/) and fill the variables with the
/// needed values as described on the values.
class Config {
  /// Holds the URL of a Firebase Realtime Database
  static const serverUrl = "";

  /// Holds the API Key (found unter Project settings/Web API Key)
  static const apiKey = "";
}
